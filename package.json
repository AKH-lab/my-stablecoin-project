cat > package.json << 'EOF'
{
  "name": "my-stablecoin-project",
  "version": "1.0.0",
  "description": "Stablecoin and PancakeSwap pool contracts for BSC",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network bsc",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network bscTestnet",
    "verify": "npx hardhat verify"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "@pancakeswap/v3-core": "^1.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.0.0",
    "@nomicfoundation/hardhat-verify": "^1.0.0"
  },
  "keywords": [
    "stablecoin",
    "bsc",
    "pancakeswap",
    "solidity",
    "smart-contract",
    "defi"
  ],
  "author": "AKH-lab",
  "license": "MIT"
}
EOF
